mocked_apis:
  apis:
    google_home:
      url: '/google'
      http:
        request:
          method: 'GET'
        response:
          strategy: string
          value: "{invalid JSON format string}"
