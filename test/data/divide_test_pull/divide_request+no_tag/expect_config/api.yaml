# Swagger API documentation config from Python FastAPI library
name: ''
description: ''
mocked_apis:
  template:
    activate: true
    load_config:
      includes_apis: True
      order:
        - 'apis'
        - 'apply'
        - 'file'
    values:
      base_file_path: './test/data/divide_test_pull/divide_http+no_tag/'
      api:
        config_path_format: '**-api'
      http:
        config_path_format: '**-http'
      request:
        config_path_format: '**-request'
      response:
        config_path_format: '**-response'
  apis:
    get_foo:
      url: '/foo'
      http:
        response:
          strategy: object
          properties: [ ]
